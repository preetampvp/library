export const ParamMissingError = "One or more of the required parameters was missing.";
export const ProductionEnv = "production";
export const DevelopmentEnv = "development";

export enum SearchType {
  title,
  category,
  author,
  description,
  isbn,
}
